import bcrypt from 'bcrypt';
import client from "../../client";

export default {
    Mutation: {
        createAccount: async (_, {
            name,
            email,
            company,
            password,
        }) => {
            try {
            } catch (e) {

            }
        },
    }
};